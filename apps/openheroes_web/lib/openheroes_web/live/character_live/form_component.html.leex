<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "character-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :level %>
  <%= number_input f, :level %>
  <%= error_tag f, :level %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
